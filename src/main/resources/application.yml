spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true

mybatis-plus:                                    # ← 改这里
  mapper-locations: classpath*:mapper/*.xml      # classpath* 更安全
  global-config:
    db-config:
      id-type: auto