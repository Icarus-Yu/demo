spring:
  datasource:
    # H2 内存数据库的连接地址和默认账号
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  # 让 Spring Boot 启动时自动执行我们写的建表 SQL 脚本
  sql:
    init:
      mode: always